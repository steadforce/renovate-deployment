renovate:
  # sub chart values
  cronjob:
    schedule: '*/1 * * * *'

  env:
    LOG_LEVEL: debug

  redis:
    architecture: standalone
    master:
      resources:
        limits: {}
        requests: {}
    replica:
      resources:
        limits: {}
        requests: {}
  resources: ~
