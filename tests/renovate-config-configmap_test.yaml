suite: renovate config configmap
release:
  name: renovate
templates:
- charts/renovate/templates/config.yaml
values:
- ../values-subchart-overrides.yaml
tests:
- it: matches the local renovate config configmap snapshot
  values:
  - ../values-local.yaml
  asserts:
  - matchSnapshot: {}
- it: matches the development renovate config configmap snapshot
  values:
  - ../values-development.yaml
  asserts:
  - matchSnapshot: {}
- it: matches the production renovate config configmap snapshot
  values:
  - ../values-production.yaml
  asserts:
  - matchSnapshot: {}
- it: renders the renovate config data for local
  values:
  - ../values-local.yaml
  asserts: &dataAssert
  - equal:
      path: data
      value:
        config.json: |-
          {
             "autodiscover": true,
             "onboardingConfig": {
               "$schema": "https://docs.renovatebot.com/renovate-schema.json",
               "extends": [
                 "config:recommended",
                 ":dependencyDashboard"
               ],
               "postUpdateOptions": [
                 "helmUpdateSubChartArchives"
               ]
             },
             "packageRules": [
               {
                 "matchDatasources": [
                   "pypi"
                 ],
                 "matchUpdateTypes": [
                   "minor",
                   "patch"
                 ],
                 "groupName": "{{manager}} dependencies non-major",
                 "groupSlug": "dependencies-non-major",
                 "additionalBranchPrefix": "{{manager}}-"
               }
             ]
          }
- it: renders the renovate config data for development
  values:
  - ../values-development.yaml
  asserts: *dataAssert
- it: renders the renovate config data for production
  values:
  - ../values-production.yaml
  asserts: *dataAssert
